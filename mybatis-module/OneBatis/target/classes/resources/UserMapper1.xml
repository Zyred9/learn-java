<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.test.mapper.UserMapper">


    <select id="getUserPage" resultType="com.example.test.entity.User">
        SELECT id, user_name, password, address, phone FROM t_user where phone = #{phone}
    </select>


    <insert id="inertUser" parameterType="com.example.test.entity.User">
        INSERT INTO t_user (user_name, password, address, phone) VALUES(#{userName}, #{password}, #{address}, #{phone})
    </insert>


    <update id="updateUser" parameterType="com.example.test.entity.User">
        UPDATE t_user SET user_name=#{userName}, password = #{password} WHERE id= #{id};
    </update>


    <delete id="deleteUser" parameterType="java.lang.String">
        Delete from t_user where id = #{id}
    </delete>
</mapper>